# post-quantum-algorithms
私钥公钥基于格的一个密码

install dilithium-py and kyber-py, them the program can run, and if run report wrong, use terminal to run, remember cd the right location


code for kyber-py
python3-m venv.venv
source.venv/bin/activate
python -m pip install--upgrade pip
python -m pip install kyber-py
python -m pip install dilithium-py

